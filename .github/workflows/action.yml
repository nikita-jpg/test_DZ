name: Test action
on:
  workflow_call:
    outputs:
      last-test-status:
        description: "The status of last test"
        value: ${{ 'true' }}
#  push:
jobs:
  test-job:
    runs-on: ubuntu-latest
    outputs:
      test-status-react: ${{ steps.test-react.output.triggered }}
    steps:
    - uses: actions/checkout@v2
      id: test-react
#
#    - uses: actions/setup-node@v1
#      name: 'setup node'
#      with:
#        node-version: '16.16.0'
#
#    - name: 'install'
#      run: npm i
#
#    - name: 'test lint'
#      run: npm run lint
#
#    - name: 'test ts'
#      run: npm run ts
#
#    - name: 'test react'
#      run: npm run test
